# وضعیت پروژه‌ها — منبع حقیقت

> آخرین ممیزی: **۲۰۲۶-۰۸-۰۶**
>
> این فایل، ثبت دائمی وضعیت واقعی همه‌ی پروژه‌های `F:\Claude` است.
> دلیل وجودش: وقتی یک سشن قطع می‌شه، کل حافظه‌ی کاری از بین می‌ره و
> دوباره باید از صفر کشف کرد چی ساخته شده و چی نه. این فایل اون حافظه رو
> روی دیسک نگه می‌داره.

## قوانین نگه‌داری این فایل

- هر ادعا اینجا باید **روی دیسک قابل تأیید** باشه. حدس و خوش‌بینی ممنوع.
- «در توسعه» یعنی **در ۳۰ روز گذشته روش کار شده**. غیر از این → «متوقف».
- پوشه‌ای که فقط سند طراحی داره و صفر خط کد → «طراحی‌شده»، نه «در توسعه».
- بعد از هر تغییر مهم توی هر پروژه، ردیف مربوطه اینجا آپدیت بشه.
- `data/content.ts` (چیزی که روی armyin.ir نمایش داده می‌شه) باید همیشه با
  این فایل هم‌خوان باشه. اگه اختلاف دیدی، **این فایل ملاکه**.

---

## جدول خلاصه

| پروژه | نوع | وضعیت واقعی | تو سایت | آخرین فعالیت |
|---|---|---|---|---|
| لانه مترووکه | اتاق فرار | فعال (اینستاگرام ۲۴.۶K) | ✅ | جاری |
| EscapeHome | پلتفرم | **لایو** — escapehome.ir | ✅ | ۲۰۲۶-۰۷-۲۸ |
| دات‌وان گلد | مشتری | **لایو** — dotone.gold | ❌ عمداً | ۲۰۲۶-۰۷-۲۹ |
| Questa (بازی ۳بعدی) | بازی | **آماده انتشار** — فقط Play Console مونده | ✅ | ۲۰۲۶-۰۸-۰۶ |
| دستیار معاملاتی | ابزار | **لایو** — ۲۴/۷ روی سرور | ✅ | ۲۰۲۶-۰۸-۰۶ |
| محتوای سایبان | مشتری | **تحویل شده** | ✅ | ۲۰۲۶-۰۷-۲۹ |
| Questa (پلتفرم) | پلتفرم | در توسعه | ذیل Questa | ۲۰۲۶-۰۸-۰۵ |
| کوئستا Unity | بازی | در توسعه — vertical slice ساخته شده | ✅ (تازه اضافه شد) | ۲۰۲۶-۰۷-۲۴ |
| پلتفرم بازی تلگرام | بازی | در توسعه | ✅ | ۲۰۲۶-۰۷-۲۲ |
| لانه متروکه Unity | بازی | در توسعه (تک‌نفره شد) | ✅ | ۲۰۲۶-۰۷-۲۲ |
| دربار مرینت | مشتری | ساخته شده، بدون لینک زنده | ✅ | ۲۰۲۶-۰۷-۰۲ |
| نفس نو | پلتفرم | پروتوتایپ HTML + سند کامل | ✅ (تازه اضافه شد) | ۲۰۲۶-۰۸-۰۳ |
| زیرنویس‌یار | ابزار | پروتوتایپ | ✅ | ۲۰۲۶-۰۷-۰۵ |
| بات دانلودر | ابزار | کد در پوشه‌ی دیگری | ✅ | ۲۰۲۶-۰۷-۲۲ |
| emtehanat.net | — | لایو، ولی تصمیم‌نگرفته | ❌ عمداً | ۲۰۲۶-۰۸-۰۱ |
| مقایسه‌گر مو و تتو | ابزار | دمو | ذیل «ایده‌ها» | ۲۰۲۶-۰۷-۰۸ |
| Players | ابزار | کار می‌کنه | ❌ | ۲۰۲۶-۰۷-۰۴ |
| بات رفرال | ابزار | کامل | ❌ | ۲۰۲۶-۰۶-۲۹ |
| پلتفرم تعمیرگاه | پلتفرم | **متوقف** (۷ هفته) | ✅ | ۲۰۲۶-۰۶-۱۸ |
| مارکت‌پلیس اکانت | پلتفرم | **فقط سند، صفر کد** | ✅ | ۲۰۲۶-۰۷-۱۴ |
| آموزش هوش مصنوعی | پلتفرم | ایده | ✅ | — |
| بازماند تنها | اتاق فرار | ایده | ✅ | — |

---

## لایو و تحویل‌شده

### EscapeHome — `Escapehome - site`
دایرکتوری اتاق‌های فرار مشهد با رزرو آنلاین. Next.js + Supabase، ۳۶۱ فایل،
`.vercel` داره، ریپو: `Arminap99/Escapehome---site`.
آخرین کامیت: ۲۰۲۶-۰۷-۲۸ («Add SEO copy to hero and room titles, harden gitignore»).
**وضعیت: لایو روی escapehome.ir.** کاری فوری نداره.

### دات‌وان گلد — `BabakSite`
سایت فروش شمش طلای ۲۴ عیار (۹۹۹.۹) در شش وزن، با موتور قیمت‌گذاری زنده،
بسته‌بندی ضدجعل، بازخرید تضمینی و ارسال بیمه‌شده. Next.js + Supabase، ۱۹۶ فایل،
`.vercel` داره. آخرین کامیت: ۲۰۲۶-۰۷-۲۶.
منطق قیمت فقط توی `lib/gold.ts` و `lib/products.ts` زندگی می‌کنه.
**وضعیت: لایو روی dotone.gold — تأیید شد.**

**روی سایت شخصی نمایش داده نمی‌شه — تصمیم آرمین، ۲۰۲۶-۰۸-۰۶.**
یه بار اضافه شد و بلافاصله به‌درخواست خودش برداشته شد. اگه بعداً کسی خواست
دوباره اضافه‌ش کنه، اول بپرس.

### Questa — بازی سه‌بعدی — `Questa 3D game`
۲۰۰۲ فایل. **بازی تمومه.** طبق `handoff/README.md`:
تست خودکار کامل پاس، صفر باگ شناخته‌شده، باندل `questa-release.aab` امضاشده،
۹ اسکرین‌شات، تریلر ۳۱ ثانیه‌ای، آیکون ۵۱۲، feature graphic، privacy policy
و متن‌های فارسی/انگلیسی فروشگاه — همه آماده.
آخرین آپدیت محتوا (۲۰۲۶-۰۸-۰۶): ۵ لباس، ۲ دشمن جدید (Screamer و Exploder)،
۲ سلاح (مولوتوف و کمان)، و درجه‌سختی Nightmare.
**تنها کار باقی‌مانده: حساب Play Console و فرایند ارسال.** هیچ تغییری در خود اپ لازم نیست.
کلید امضا در `android/questa-release.keystore` — هر آپدیت بعدی حتماً با همین کلید.

### دستیار معاملاتی — `Trader bot`
۲۴ ساعته روی سرور (AnyDesk) با Task Scheduler اجرا می‌شه.
شامل: بات تلگرام، تحلیل چارت با Claude Vision، اسکنر خودکار بازار،
ژورنال معامله، و موتور قوانین (`RULES.md`) که از بررسی تصویری ۱۴۲ معامله‌ی
واقعی استخراج شده — Win Rate ۷۳.۲٪، Expectancy ‎+۱.۴۰R.
یافته‌ی کلیدی: ورود counter-trend صفر از ۸ برد داشت؛ ست‌آپ‌های ساده‌ی هم‌جهت
با ترند ۹۴–۹۶٪ در برابر ست‌آپ‌های پترن‌محور ۲۸.۶٪.
**مسئله‌ی باز:** اسکن خودکار سیگنالی پست نمی‌کنه. تشخیص داده شد که مرحله‌ی
مکانیکی سالمه (روی داده‌ی زنده ۳ کاندید پیدا کرد)؛ مرحله‌ی بعدی تست مرحله‌ی
AI روی همون کاندیداهاست.

### محتوای سایبان — `Saiban Content`
۱۹۱ فایل، ۴ سناریوی تبلیغاتی با کاراکتر اختصاصی، به‌همراه `final-exports`.
**تحویل شده.**

---

## در توسعه‌ی فعال

### Questa — پلتفرم — `Questa-platform-main`
`Questa-platform` (وب) + `Questa-Mobile` + `character-assets` + برنامه‌ی محتوا
(`CONTENT-PLAN.md/html/pdf`) + `CHASE-SERIES.md`. `vercel.json` داره.
آخرین کار محتوایی: ۲۰۲۶-۰۸-۰۵.

### کوئستا Unity — `Questa Unity`
بازی open-world سبک low-poly (Unturned/Minecraft) روی نقشه‌ی واقعی مشهد.
ریپو: `Arminap99/Questa-Unity`، آخرین کامیت ۲۰۲۶-۰۷-۲۴.

**توجه: `PROJECT_BRIEF.md` کهنه‌ست.** آخرین خطش می‌گه «هیچ کدی هنوز نیست» —
این دیگه درست نیست. روی دیسک ۱۶ اسکریپت C# هست:

| پوشه | اسکریپت‌ها |
|---|---|
| `Player` | ThirdPersonController، ThirdPersonCamera |
| `Vehicle` | SimpleCarController، VehicleEntryExit |
| `Quest` | QuestManager، QuestData، QuestStep |
| `Interaction` | IInteractable، InteractableBuilding، PlayerInteractor |
| `WorldGen` | GeoJsonParser، MapProjection، CityGenerator، RoadMeshBuilder، BuildingMeshBuilder، EarClipTriangulator |

یعنی vertical slice (پیاده‌روی + ماشین + کوئست + تولید procedural شهر از GeoJSON)
واقعاً پیاده‌سازی شده. **کار لازم: خط آخر بریف اصلاح بشه.**

تصمیم معماری: دنیا و داستان مشترک با Questa، ولی کدبیس و پلتفرم کاملاً جدا.
Questa (مینی‌اپ تلگرام) نقش قیف ورودی رو داره.

### پلتفرم بازی تلگرام — `board games telegram`
از «یک بازی ترسناک» تغییر جهت داده به **پلتفرم چندبازی** با هسته‌ی مشترک
(دوز، ۲۰ سوالی، جاسوس و…). Next.js + Supabase. آخرین کار ۲۰۲۶-۰۷-۲۲.
**نکته:** کد `downloader-bot` هم داخل همین پوشه‌ست، نه توی پوشه‌ی خودش.

### لانه متروکه — تجربه‌ی Unity — `Co Op games`
**تصمیم ۲۰۲۶-۰۷-۲۱: co-op کنار گذاشته شد، بازی تک‌نفره شد.** دلیل: تست کردن
تنهایی غیرممکن بود و نبود mouse-look اذیت می‌کرد. معماهایی که فرض دو نفره داشتن
با بازتر کردن پنجره‌ی زمانی، solo-doable شدن (نه بازطراحی کامل).
نام پوشه (`Co Op games`) دیگه گمراه‌کننده‌ست. ۲۶٬۲۹۶ فایل (پروژه‌ی Unity).

### نفس نو — `tark sigar`
اپ ترک سیگار. تفاوت با بقیه: به‌جای عدد و streak، **تصویر آناتومی زنده از بدن**
که با ثبت روزانه‌ی عادت‌ها بهبود پیدا می‌کنه (ریه از خاکستری به صورتی).
سند فنی کامل ۲۵KB در `README.md` + دو پروتوتایپ HTML (`index.html` ۵۳KB،
`nafas-no.html` ۲۰KB).
استک پیشنهادی: React + Vite + Tailwind + Zustand + Dexie + date-fns-jalali + Capacitor.
الزامات بومی: تقویم شمسی، اعداد فارسی، RTL کامل، تومان، **کار کامل آفلاین**.
**کار لازم: تبدیل پروتوتایپ HTML به پروژه‌ی واقعی Vite.**

---

## متوقف

### پلتفرم تعمیرگاه — `Repair - app`
«بهترین تعمیرگاه نزدیک من» — نقشه‌ی مشکل‌محور با امتیاز رضایت به تفکیک نوع مشکل.
۱۵۱ فایل، `PROJECT_PLAN.md` و `schema.sql` موجوده.
**از ۲۰۲۶-۰۶-۱۸ دست‌نخورده — حدود ۷ هفته.** یا برگرد سراغش یا رسماً کنار بذارش؛
نگه داشتنش با برچسب «در توسعه» فقط خودفریبیه.

### دربار مرینت — `darbar resturant`
سایت رستوران، ساخته و static-export شده (`out/`)، ریپو:
`Arminap99/darbar-marinet-site`. آخرین کامیت ۲۰۲۶-۰۷-۰۲.
**سؤال باز: این سایت جایی لایو شده؟** اگه آره، لینکش باید بره تو `content.ts`.

---

## ایده و طراحی

### مارکت‌پلیس اکانت — `jahooshi`
بات تلگرامی فروش اکانت اشتراکی با کیف‌پول داخلی، شارژ تومانی/USDT و تحویل
خودکار یا دستی. **پوشه فقط و فقط یک فایل `CLAUDE.md` داره. صفر خط کد.**
تا قبل از این ممیزی روی سایت برچسب «در توسعه» داشت — که واقعیت نداشت.

### مقایسه‌گر مو و تتو — `hair and tattoo`
دموی مقایسه‌ی خروجی چند مدل AI برای پیشنهاد مدل مو/تتو + لاگ انتخاب‌ها
برای ساخت یک بنچمارک شخصی. طبق `HAIR_TATTOO_MVP_PLAN.md` صراحتاً
**محصول نهایی نیست، ابزار یادگیریه**.
نکته: دو تا از «ایده‌ها»ی سایت (چه مویی به من میاد / چه تتویی بزنم) دیگه
صرفاً ایده نیستن — پشتشون دمو هست.

### بازماند تنها / آموزش هوش مصنوعی
ایده، بدون کد. برچسب سایت درسته.

---

## ابزارهای کاری — روی سایت نیستند (و لازم هم نیست باشند)

| پوشه | چیه | وضعیت |
|---|---|---|
| `Players` | بات جستجوی دیتابیس روی CSV/SQLite، ریپو `Players-` | کار می‌کنه |
| `nahayi_bot` | بات رفرال تلگرام با عضویت اجباری و جایزه‌ی خودکار | کامل، نیاز به VPS |
| `Rezayat_Moshtari` | اسکریپت PowerShell ارسال ۱۲۰ نمونه‌متن رضایت به کانال | ابزار یک‌بارمصرف |
| `zarinpal-proxy` | پروکسی درگاه پرداخت | زیرساخت |
| `VPN` | اسکریپت نصب VPN | زیرساخت |
| `sss` | صفحه‌ی reveal پسوردی مسابقه‌ی Questa | یک‌بارمصرف |
| `ArminNewPagesVideos` | کانسپت‌های محتوا و کاراکتر | یادداشت |
| `backups` | بکاپ نسخه‌ی اول همین سایت (`my-self-v1-sonnet`) | آرشیو |
| `Trade` | نسخه‌ی قدیمی و متروک دستیار معاملاتی | جایگزین شده با `Trader bot` |
| `Cowork`, `first one`, `app e jadiddds` | اسکرچ و دانلودی | دور ریختنی |

---

## emtehanat.net — `omid site` — نیاز به تصمیم تو

عمداً روی سایت شخصی **اضافه نشد**، چون دو تا چیز باید اول روشن بشه:

1. **ماهیتش.** این یک سایت آموزشی معمولی نیست. یک صفحه‌ی محتوای پسورددارِ
   (`emtehanat.net/q/…`) که فایل‌های `content-riazi`، `content-oloom`،
   `content-joghrafia`، `content-hesaban` و `launcher/سوالات.pdf` رو پشت پسورد
   سرو می‌کنه، با آپدیت لحظه‌ای محتوا روی Redis بدون redeploy و قفل IP بعد از
   ۵ تلاش غلط. همون کدبیس `sss` (questa-reveal) دوباره استفاده شده.
   قبل از اینکه اسمت رو عمومی بهش وصل کنیم، خودت باید بگی این دقیقاً چیه.

2. **یک مسئله‌ی امنیتی واقعی.** فایل `docs/RUNBOOK.md` **کامیت شده توی گیت**
   و **پسورد پروداکشن سایت به‌صورت متن ساده داخلشه**. همین‌طور
   `docs/access-list.txt`. `.gitignore` این ریپو `.env*` رو درست ignore می‌کنه —
   یعنی رمز عمداً از env بیرون آورده شده و توی docs گذاشته شده.
   ریپوی `Arminap99/omid-site` الان از بیرون ۴۰۴ می‌ده (یعنی private یا حذف‌شده)،
   پس فعلاً فاش نشده. ولی اگه روزی public بشه، پسورد لو می‌ره.
   **پیشنهاد: رمز رو از `RUNBOOK.md` بردار، ببرش تو `.env.local`، و رمز فعلی رو عوض کن.**

---

## کارهای باقی‌مانده — چک‌لیست

**فوری**
- [ ] پسورد پروداکشن رو از `omid site/docs/RUNBOOK.md` بردار و عوضش کن
- [ ] Questa: حساب Play Console بساز و بازی رو بفرست (تنها مانع انتشار)
- [ ] Questa: `store/privacy.html` رو یه جای عمومی هاست کن و لینکش رو بگیر

**سایت شخصی**
- [x] `content.ts` با واقعیت هم‌خوان شد (۲۰۲۶-۰۸-۰۶)
- [ ] برای ۲ پروژه‌ی تازه‌اضافه‌شده تصویر بساز:
      `questa-unity.png` و `nafas-no.png` توی `public/images/projects/` —
      فعلاً `image: null` دارن و با ایموجی و گرادیان رندر می‌شن
      (سالمه، ولی از بقیه ضعیف‌تر دیده می‌شه)
- [ ] تصمیم بگیر emtehanat.net روی سایت بیاد یا نه
- [ ] چک کن دربار مرینت لینک زنده داره یا نه؛ اگه داره اضافه کن

**بدهی فنی پروژه‌ها**
- [ ] خط آخر `Questa Unity/PROJECT_BRIEF.md` رو اصلاح کن (می‌گه کد نیست، ولی هست)
- [ ] `Trader bot`: مرحله‌ی AI رو روی کاندیداهای اسکنر تست کن تا معلوم بشه چرا
      سیگنالی پست نمی‌شه
- [ ] پوشه‌ی `Co Op games` رو به اسم درست تغییر بده (بازی دیگه co-op نیست)
- [ ] `downloader-bot` رو از `board games telegram` جدا کن، یا پوشه‌ی
      `youtube and instagram DOWNLOADER` رو حذف کن
- [ ] تکلیف `Repair - app` رو روشن کن: ادامه یا بایگانی
